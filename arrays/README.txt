1. Criar um vetor A com 5 elementos inteiros. Construir um vetor B de mesmo tipo e tamanho e com os "mesmos" elementos do vetor A, ou seja B[i] = A[i]
Resolução: array1

2. Criar um vetor A com 8 elementos inteiros. Construir um vetor B de mesmo tipo e tamanho e com os elementos d vetor A multiplicadfos por 2, ou seja: B[i] = A[i] * 2.
Resolução: array2

3. Criar um vetor A com 15 elementos inteiros. Construir um vetror B de mesmo tipo e tamanho, sendo que cada elemento do vetor B deverá ser o quadrado do respectivo elemento de A, opu seja: B[i] = A[i] * A[l].
Resolução: array3

4. Criar um vetor A com 15 elementos inteiros. Construir um vetror B de mesmo tamanho, sendo que cada elemento do vetor B deverá ser a raiz quadrada do respectivo elemento de A, ou seja: B[i] = sqrt(A[i]);
Resolução: array4

5. Criar um vetor A com 10 elementos inteiros. Construir um vetror B de mesmo tipo e tamanho, sendo que cada elemento do vetor B deverá ser respectivo elemento de A multiplicado por sua posição (ou índice), ou seja: B[i] = A[i] * i.
Resolução: array5